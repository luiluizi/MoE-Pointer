scenario: synthetic-large2
interruptable: False
use_sp: True

n_frame: 128
n_requested_frame: 96
n_vehicle: 50
n_node: 300

n_init_requests: 50
n_norm_requests: 500
max_consider_requests: 200

max_capacity: 3
dist_distribution: points
max_dist: 30
dist_cost: 0.0

info_batch_size: 0
debug: False

# 环境应约满足以下等式。
# max_dist/2 * (n_init_requests + n_norm_requests) == n_vehicle * n_frame * (max_capacity / 2)
# max_value 下的reward估算：n_vehicle * n_frame * (max_capacity / 2)
# python3 mat/scripts/train/train_vrp.py --n_rollout_threads=128 --mini_batch_size=128